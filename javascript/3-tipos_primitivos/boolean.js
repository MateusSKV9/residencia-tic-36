console.log('=====> Boolean <=====');
let ativo = true;
let desativado = false;

console.log(`Ativo && Ativo: ${ativo && ativo}`);
console.log(`Desativado && Desativado: ${desativado && desativado}`);
console.log(`Ativo && Desativado = ${ativo && false}`);
